We employ a hybrid transport architecture that combines primary k=3 Forward Error Correction (FEC) with a targeted Automatic Repeat-Request (ARQ) NACK fallback. To strictly obey the 2.0x bandwidth cap, the sender maintains a dynamic budget tracker that allows NACKs and retransmissions to inherently consume bandwidth, organically suppressing FEC redundancy only when ARQ actively steals the budget. Our receiver is completely stateless and bufferless, intentionally offloading jitter absorption to the harness player's absolute arrival deadline to avoid complex local blocking. To prevent GC latency spikes during high-frequency playout, we achieved a zero-allocation hot loop by replacing all maps and dynamic slices with statically sized array structs and ring buffers. The receiver evaluates NACK conditions at a decoupled frequency limit (10ms) rather than on every packet arrival, drastically reducing CPU arithmetic overhead. Grade this submission at a delay_ms of 140. This delay optimally absorbs the maximum theoretical 80ms network jitter and gives our k=3 redundancy exactly enough time to arrive, mathematically guaranteeing survival against burst drops up to 3 packets long. While 140ms isn't enough to guarantee a full worst-case ARQ round trip, the delay distribution often allows NACKs to resolve quickly, allowing our ARQ net to probabilistically rescue massive >3 packet bursts without needing a 300ms delay. This architecture will break and fail the miss cap only if a hidden grading profile contains sustained, continuous packet loss bursts exceeding 3 packets where the round-trip time simultaneously spikes above 60ms for the NACK.
